# 🧩 Генератор договоров (.docx)

Веб-приложение на **FastAPI + чистом HTML/CSS/JS** для создания договоров на основе шаблона Word (`.docx`).

## ⚙️ Возможности
- Веб-форма для ввода данных клиента и параметров договора.  
- Три таблицы с выбором услуг:
  - **Интернет-услуги** — выбор скорости, авто-подстановка цен.  
  - **Дополнительные услуги** — выбор услуги, автоматическая аренда.  
  - **Оборудование** — выбор устройства, ввод количества и адреса, автоматический расчёт итоговой суммы.  
- Генерация `.docx` с помощью `docxtpl`.  
- Автоматическая загрузка готового файла.  
- Уведомление об успехе (popup-окно в сине-сером стиле).

## 🧱 Структура проекта
```
project/
├── server.py                     # FastAPI сервер
├── agreements_templates/         # Папка с шаблонами договоров
│   └── main.docx                 # Шаблон договора с ИП
├── results/                      # Сюда сохраняются сгенерированные файлы
└── static/
    ├── index.html                # Основная страница
    ├── style.css                 # Стили
    └── script.js                 # JS-логика
```

## 🚀 Установка и запуск

### 1. Создать папку проекта
Распаковать архив с проектом и через терминал зайти в папку проекта
```bash
cd agreement_generator
```

### 2. Проверить наличие Python 3
```bash
python3 --version
```
Если Python 3 то его нужно установить

### 3. Создать виртуальное окружение
```bash
# Создание окружения
python3 -m venv .venv
# Активация на macOS / Linux
source .venv/bin/activate
# Активация на Windows
.venv\Scripts\activate
```

### 4. Установить зависимости
```bash
pip3 install fastapi uvicorn docxtpl jinja2 python-multipart
```

### 5. Запустить сервер
```bash
python3 -m uvicorn server:app --reload --port 8010
```

### 6. Открыть приложение
Перейдите в браузере по адресу:
```
http://127.0.0.1:8010
```
Введите данные, нажмите **«Сформировать договор»**, дождитесь скачивания файла.  
После успешной генерации появится всплывающее окно с сообщением об успехе.
